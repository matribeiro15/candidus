{{!--
Site banner partial

This partial shows the cover image of the site.
It is used on the index page, as well as a fallback for the tag page if no tag image is maintained.
If no cover image is maintained, nothing, not even the site's name, will be displayed.
--}}

{{#if @site.cover_image}}
<header class='banner'>
	<div class='banner__text mx-6 mb-2 py-1'>
		<h1 class='banner__title'>{{@site.title}}</h1>
		<h2 class='banner__description hidden-mobile'>{{@site.description}}</h2>
	</div>
	<img class='banner__image image--banner' srcset='{{img_url @site.cover_image size=' s'}} 300w, {{img_url @site.cover_image size='m' }} 600w, {{img_url @site.cover_image size='l' }} 1000w, {{img_url
		@site.cover_image size='xl' }} 2000w' sizes='100vw' src='{{img_url @site.cover_image size=' xl'}}' alt='{{site.title}}' />
</header>
{{else}}
<h1 class="hidden">{{@site.title}}</h1>
<h2 class="hidden">{{@site.description}}</h2>
{{/if}}