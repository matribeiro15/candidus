{{!< default}} <section class='container container--page'>
	<h1 class='mb-2'>{{t '404 - Page not found'}}</h1>
	<p class='mb-4'>{{t 'The page you are looking for does not exist'}}</p>

	<a class='btn btn--primary btn--wider mb-1' href='#' onclick='window.history.back()'>{{t
		'To the previous page'
		}}</a>
	<a class='btn btn--wider' href='{{@site.url}}'>{{t 'To the front page'}}</a>
	</section>